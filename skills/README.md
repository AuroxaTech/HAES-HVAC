# Skills (portable)

Reusable guidelines for AI-assisted development. Copy this folder to other projects so the same standards apply everywhere.

- **PromptEngineeringSkills.md** — Use when editing system prompts or knowledge bases. Keeps prompts minimal and pushes SOPs/reference into KB. Reference this file (or open it) when working on prompt changes; in this project, the Cursor rule `.cursor/rules/prompt_engineering_skills.mdc` applies it automatically when editing `doc/vapi/system_prompt_*.md` or `doc/vapi/kb/**`.
